import SwiftUI

/// App entry point. Renders the root navigation surface.
///
/// This is scaffolding only — screens are placeholders until their feature
/// specs (see `docs/specs/`) are implemented.
@main
struct OpenSpiderSolitaireApp: App {
    var body: some Scene {
        WindowGroup {
            RootView()
        }
    }
}
