# OpenSpiderSolitaireUITests

Intentionally empty for now.

Per `CLAUDE.md` ("DO NOT write UITests during scaffolding phase"), there is no
UI test target wired in `project.yml` and no tests here yet. This folder is a
placeholder that reserves the standard location.

When critical user flows are ready to be covered (per the PRD), add a
`bundle.ui-testing` target to `project.yml` pointing at this folder and write
the UI tests here.
